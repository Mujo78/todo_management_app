import { Typography } from "@mui/material";
import React from "react";

const LoginForm: React.FC = () => {
  return (
    <>
      <Typography variant="h4">Log in to You Account</Typography>
    </>
  );
};

export default LoginForm;
